[include]
    path = ~/.gituser

[color]
    ui = true
    
[push]
    default = current

[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    fe = fetch
    re = rebase
    last = log --stat -1
    list = log --pretty=full
    ffs = flow feature start
    fff = flow feature finish
    frs = flow release start
    frf = flow release finish
    fhs = flow hotfix start
    fhf = flow hotfix finsih